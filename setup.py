from setuptools import setup

install_requires = [
]

packages = [
    "py_restart"
]

setup(
    name='py_restart',
    version='0.0.0',
    packages=packages,
    install_requires=install_requires,
)